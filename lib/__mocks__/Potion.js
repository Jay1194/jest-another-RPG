module.exports = function() {
    //mock doesn't need to incorporate any random logic; it just needs to return valid values that the Player object can later use.
    this.name = 'health';
    this.value = 20;
};